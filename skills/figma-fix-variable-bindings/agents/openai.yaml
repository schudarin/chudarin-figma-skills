interface:
  display_name: Figma Fix Variable Bindings
  short_description: Scan a Figma file for detached, orphaned, or hardcoded variable bindings — typography, spacing, radii, colours — and rebind them to library tokens.
  icon: figma
  brand_color: "#1E1E1E"
policy:
  allow_implicit_invocation: true
